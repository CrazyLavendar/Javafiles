public class DoubleEndedArrayQueue extends ArrayQueue {

    /*
     * Double ended queue where dequeue can be happen at both the ends
     * 
     */
    public DoubleEndedArrayQueue(int capacity) {
        super(capacity);
    }
}
