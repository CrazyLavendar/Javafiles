
            // for (int i = 0; i < len / 2; i++) {
            //     int temp = list.get(i);
            //     list.set(i, list.get(len - i - 1));
            //     list.set(len - 1- i, temp);
            // }